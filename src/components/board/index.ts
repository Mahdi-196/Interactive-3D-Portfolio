/**
 * Barrel export for the Interactive Detective Board
 * Main entry point for importing the board component
 */

export { InteractiveDetectiveBoard } from './InteractiveDetectiveBoard';
export type { InteractiveDetectiveBoardProps, CaseFile, ProjectDetail, ZoomedPaper, RespawnPaper, SideProject } from './types';
